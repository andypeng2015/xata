apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: gateway-configmap-reader
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    resourceNames:
      - ipfiltering
    verbs:
      - get
      - list
      - watch
