apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: xata

helmCharts:
  - name: projects
    releaseName: projects
    namespace: xata
    valuesFile: values.yaml

helmGlobals:
  chartHome: ../../../../charts
